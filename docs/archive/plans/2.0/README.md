# 2.0 Archive Notes

`2.0` is complete and no longer an active planning line.

This folder preserves the historical `2.0` design and implementation documents for reference and auditability.

Important interpretation rules:

- Not every directional idea mentioned in late `2.0` documents was implemented.
- Those unfinished ideas are intentionally not carried forward as `2.0` work.
- Any future continuation must be replanned under `2.1+`, not revived as `2.0G`, `2.0H`, or similar suffixes.
